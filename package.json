{
  "name": "vault-proto",
  "version": "0.0.1",
  "scripts": {
    "frontend": "cd frontend && npm run start",
    "server": "nodemon server.js",
    "chain": "cd blockchain && npm start",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run frontend\" \"npm run chain\""
  },
  "dependencies": {
    "axios": "^0.18.1",
    "body-parser": "^1.20.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
